import { StyleSheet } from 'react-native'

import { THEME } from '../../../constants'

export const styles = StyleSheet.create({
  linkButtonText: {
    color: THEME.DODGER_BLUE,
    fontSize: 14,
    lineHeight: 16,
    letterSpacing: 0.75,
  }
})
